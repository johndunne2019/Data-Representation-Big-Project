CREATE DATABASE stockbullfinder;

CREATE TABLE bulls (
    id INT AUTO_INCREMENT PRIMARY KEY,
    code VARCHAR(10), 
    name VARCHAR(250), 
    breed VARCHAR(3), 
    owner VARCHAR(250)
    );

use stockbullfinder 

